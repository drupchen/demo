.DS_Store
.serena
.playwright-mcp
.superpowers
floating-pecha-ui/public/data

# prepare_data: track the pipeline scripts, ignore env / cache / generated data
prepare_data/.venv/
prepare_data/__pycache__/
prepare_data/.pytest_cache/
prepare_data/output/
# Integration tests rely on local-only data (docs/export-4.json + gitignored
# manifests), so they can't run for collaborators — keep them out of the repo.
prepare_data/test_sapche_align.py
